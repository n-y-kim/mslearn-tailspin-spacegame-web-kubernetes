apiVersion: v1
kind: Service
metadata:
    name: "tailspingame27045-e97a"
    labels:
        app: "tailspingame27045-e97a"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "tailspingame27045-e97a"